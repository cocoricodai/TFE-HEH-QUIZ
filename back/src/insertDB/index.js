const { campus } = require('./campus');
const { sections } = require('./sections');
const { roles } = require('./roles');
const { blocks } = require('./blocks');
const { sectionblocks } = require('./sectionblocks');

module.exports = {
	campus,
	sections,
	roles,
	blocks,
	sectionblocks,
};
